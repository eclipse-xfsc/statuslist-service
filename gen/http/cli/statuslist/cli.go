// Code generated by goa, DO NOT EDIT.
//
// statuslist HTTP client CLI support package
//
// Command:
// $ goa gen github.com/eclipse-xfsc/statuslist-service/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	statusc "github.com/eclipse-xfsc/statuslist-service/gen/http/status/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"status (health|get-list|revoke)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "status health" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		statusFlags = flag.NewFlagSet("status", flag.ContinueOnError)

		statusHealthFlags = flag.NewFlagSet("health", flag.ExitOnError)

		statusGetListFlags        = flag.NewFlagSet("get-list", flag.ExitOnError)
		statusGetListListIDFlag   = statusGetListFlags.String("list-id", "REQUIRED", "")
		statusGetListTenantIDFlag = statusGetListFlags.String("tenant-id", "REQUIRED", "")
		statusGetListAcceptFlag   = statusGetListFlags.String("accept", "", "")

		statusRevokeFlags        = flag.NewFlagSet("revoke", flag.ExitOnError)
		statusRevokeTenantIDFlag = statusRevokeFlags.String("tenant-id", "REQUIRED", "")
		statusRevokeListIDFlag   = statusRevokeFlags.String("list-id", "REQUIRED", "")
		statusRevokeIndexFlag    = statusRevokeFlags.String("index", "REQUIRED", "")
	)
	statusFlags.Usage = statusUsage
	statusHealthFlags.Usage = statusHealthUsage
	statusGetListFlags.Usage = statusGetListUsage
	statusRevokeFlags.Usage = statusRevokeUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "status":
			svcf = statusFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "status":
			switch epn {
			case "health":
				epf = statusHealthFlags

			case "get-list":
				epf = statusGetListFlags

			case "revoke":
				epf = statusRevokeFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "status":
			c := statusc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "health":
				endpoint = c.Health()
			case "get-list":
				endpoint = c.GetList()
				data, err = statusc.BuildGetListPayload(*statusGetListListIDFlag, *statusGetListTenantIDFlag, *statusGetListAcceptFlag)
			case "revoke":
				endpoint = c.Revoke()
				data, err = statusc.BuildRevokePayload(*statusRevokeTenantIDFlag, *statusRevokeListIDFlag, *statusRevokeIndexFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// statusUsage displays the usage of the status command and its subcommands.
func statusUsage() {
	fmt.Fprintln(os.Stderr, `Status list endpoints`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] status COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    health: Health implements health.`)
	fmt.Fprintln(os.Stderr, `    get-list: Returns a status list as JSON, JWT status list, or StatusList2021 credential depending on Content-Type.`)
	fmt.Fprintln(os.Stderr, `    revoke: Revokes a credential by setting the bit at the given index.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s status COMMAND --help\n", os.Args[0])
}
func statusHealthUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] status health", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Health implements health.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "status health")
}

func statusGetListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] status get-list", os.Args[0])
	fmt.Fprint(os.Stderr, " -list-id INT")
	fmt.Fprint(os.Stderr, " -tenant-id STRING")
	fmt.Fprint(os.Stderr, " -accept STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Returns a status list as JSON, JWT status list, or StatusList2021 credential depending on Content-Type.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -list-id INT: `)
	fmt.Fprintln(os.Stderr, `    -tenant-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -accept STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "status get-list --list-id 7180610089920843089 --tenant-id \"Magni ex dignissimos facilis ut.\" --accept \"Deserunt cum et est assumenda.\"")
}

func statusRevokeUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] status revoke", os.Args[0])
	fmt.Fprint(os.Stderr, " -tenant-id STRING")
	fmt.Fprint(os.Stderr, " -list-id INT")
	fmt.Fprint(os.Stderr, " -index INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Revokes a credential by setting the bit at the given index.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -tenant-id STRING: `)
	fmt.Fprintln(os.Stderr, `    -list-id INT: `)
	fmt.Fprintln(os.Stderr, `    -index INT: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "status revoke --tenant-id \"Modi iure saepe fugiat fugiat quia rerum.\" --list-id 8306568783149687004 --index 3685248599561375189")
}
