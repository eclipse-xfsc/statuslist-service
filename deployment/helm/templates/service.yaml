apiVersion: v1
kind: Service
metadata:
  name:      {{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    {{ include "statuslistservice.selectorLabels" . }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP